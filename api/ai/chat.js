const SYSTEM=`You are Clip-E, the conversational interface for a robotic haircut prototype. Speak clearly, calmly, briefly, and naturally. Tell the user what the system is doing and answer haircut questions. Never claim to know how the user feels from facial-expression analysis. If a contextual expression signal changes, ask a neutral check-in. Never claim a physical action occurred unless application state confirms it. You do not directly control hardware. For requested haircut changes, describe the requested change and require explicit confirmation. PAUSE and STOP are deterministic application controls.`;
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!process.env.GEMINI_API_KEY)return res.status(503).json({error:'Gemini is not configured'});try{const {message,context}=req.body||{};const r=await fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent',{method:'POST',headers:{'Content-Type':'application/json','x-goog-api-key':process.env.GEMINI_API_KEY},body:JSON.stringify({system_instruction:{parts:[{text:SYSTEM}]},contents:[{role:'user',parts:[{text:JSON.stringify({message,clipEContext:context})}]}],generationConfig:{temperature:.3,maxOutputTokens:100}})});const j=await r.json();if(!r.ok)throw Error(j?.error?.message||'Gemini request failed');const text=j?.candidates?.[0]?.content?.parts?.map(x=>x.text||'').join('').trim();return res.status(200).json({text:text||'I’m here. What would you like to know?'});}catch(e){return res.status(500).json({error:e.message})}}