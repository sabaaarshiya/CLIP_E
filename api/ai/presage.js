export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!process.env.PRESAGE_API_KEY||!process.env.PRESAGE_API_URL)return res.status(503).json({available:false,error:'Presage is not configured'});return res.status(501).json({available:false,error:'Presage adapter requires the exact supported endpoint/schema from your Presage account. No facial-affect result is being fabricated.'})}